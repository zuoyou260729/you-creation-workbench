# 物品收纳（物品统计）模块 — 需求指令汇编

> 整理自本机工作记忆 `.workbuddy/memory/`（2026-09-01 ~ 2026-09-08 日志 + MEMORY.md）与本次会话记录。  
> 时间跨度：**2026-09-01 → 2026-09-08**，共 **34 个版本迭代**（v8 → v41）。

## 关于还原度的说明

- 标注 **【原文】** 的条目，是你在对话里发出的原话（完整保留）。
- 标注 **【归纳】** 的条目，来自当时的工作日志记录，是我按你的意思归纳的要点，**非逐字原文**（原始对话已随会话压缩不可完整回溯）。
- 云端历史对话检索返回 0 条结果，因此早期版本（v8–v32）只能以【归纳】形式呈现；v33 起可给出【原文】。

---


## 一、时间线总览

| 版本   | 日期    | 主题                                                | 来源        |
| ---- | ----- | ------------------------------------------------- | --------- |
| 模块创建 | 09-01 | 新增「物品统计 / 物品分类 / 到期清单」三子页（交付包：需求规格.md + 37 张参考截图） | MEMORY.md |
| v8   | 09-02 | 图标去圆底重提取 + 新增二级分类弹窗 + 选择图标弹窗改版                    | 归纳        |
| v9   | 09-02 | 手机端侧边栏改抽屉 + 二级分类图标圆形修复                            | 归纳        |
| v10  | 09-02 | 解决系统「边缘滑动返回」与抽屉呼出冲突                               | 归纳        |
| v11  | 09-02 | 根治「刷新了还是旧版」：SW 更新提示条                              | 归纳        |
| v12  | 09-03 | 手机端底部标签栏                                          | 归纳        |
| v13  | 09-03 | 物品统计 4 项修改                                        | 归纳        |
| v14  | 09-03 | 物品统计 3 项修改（含详情页改全屏档案页）                            | 归纳        |
| v15  | 09-03 | 按 11 张手机截图重新抠系统分类图标                               | 归纳        |
| v16  | 09-03 | 修手机端加载慢 + 物品数据丢失                                  | 归纳        |
| v17  | 09-04 | 添加物品表单：分类滚轮 + 图标选择器改版                             | 归纳        |
| v18  | 09-04 | 添加物品表单 3 项修复                                      | 归纳        |
| v19  | 09-04 | **物品档案页重构 + 多批次数据模型**（里程碑）                        | 归纳        |
| v20  | 09-04 | 补货入库弹框被遮挡（z-index）                                | 归纳        |
| v21  | 09-04 | 补货入库 / 编辑库存品多项修复                                  | 归纳        |
| v22  | 09-04 | 云端双向同步（GitHub）                                    | 归纳        |
| v23  | 09-04 | 三界面 UI 优化 + 置顶 + 分类模糊搜索                           | 归纳        |
| v24  | 09-04 | 我的分类页两项优化                                         | 归纳        |
| v25  | 09-05 | 档案页优化 + **修同步误删本地数据（致命 bug）**                     | 归纳        |
| v26  | 09-05 | 同步诊断 + 后端可切 GitHub/Gitee + 弱网重试                   | 归纳        |
| v27  | 09-05 | 修「编辑物品会覆盖历史入库批次」                                  | 归纳        |
| v28  | 09-06 | 七项界面/逻辑改造                                         | 归纳        |
| v29  | 09-06 | **回退 v28 第 2 条**（我误删了「批量」角标）                      | 归纳        |
| v30  | 09-06 | 正确实现：取消底部「入库」按钮                                   | 归纳        |
| v31  | 09-06 | **修正 v30 误删范围**（只删单个物品的）                          | 归纳        |
| v32  | 09-06 | 退库日期改「按批次」模型 + 6 项明细                              | 归纳        |
| —    | 09-06 | 输出《物品收纳模块详细文档》《物品收纳管理_PRD》                        | 原文        |
| v33  | 09-06 | 编辑页退库日期弹窗只显示唯一标识符                                 | 原文        |
| v34  | 09-06 | 补货弹窗 + 编辑页批次行 4 项                                 | 原文        |
| v35  | 09-06 | 补货有效期显示 + 退库日期联动/对齐                               | 原文        |
| v36  | 09-06 | 档案页退库日期默认不带出（你质疑没改）                               | 原文        |
| v37  | 09-06 | 库存档案拆成「库存档案 + 每批次档案」                              | 原文        |
| v38  | 09-06 | 修「每批次档案退库日期编辑后不保存」                                | 原文        |
| v39  | 09-07 | 补货加退库日期 + 编辑页每批次档案 + 根治 BUG                       | 原文        |
| v40  | 09-07 | 修 v39 两项回归（补货退库日期点击无反应 / 编辑按钮无反应）                 | 原文        |
| v41  | 09-08 | 6 项 UI 调整（最新）                                     | 原文        |

---

## 二、模块创建（2026-09-01）

**【归纳】** 你提供了 `D:\个人工作台\工作台交付包.zip`，内含 **需求规格.md + 37 张参考截图**，要求据此实现「物品收纳」模块，落地为三个共享 `#page-items` 的子页：

- **物品统计**（总览：总资产 / 平均每日成本 / 容量 / 清单与网格视图）
- **物品分类**（我的分类 + 系统分类，18 个一级 + 233 个二级）
- **到期清单**

同期还提出四点需求：①分类管理按钮删除 ②系统分类图标全替换 ③添加一级后默认展开子分类 ④二级图标点击高亮。

> 关键规格（后续一直沿用）：
>
> - 入库批次唯一标识符 = `YYYYMMDD入库`（按入库日期，非 uuid）
> - 系统分类固定 18 个一级 +「全部」；二级共 233 条
> - 总资产 = Σ总价；平均每日成本 = Σ(总价 ÷ (有效期 − 生产日期 + 1))；容量 = 品类数 / 9999

---

## 三、逐版本指令明细

###

**【归纳】**

1. 设计稿里图标是圆形的，**不要裁切圆**，只把圆内的图标内容抠出来（透明 PNG），再放进 App 自己的圆形容器里。
2. 「新增二级分类」弹窗按截图1 改版：标题+副标题、输入框、分类图标行、取消/保存。
3. 「选择图标」弹窗按截图2 改版：顶部标题+×、横向滚动分类 chip、5 列图标网格。

### v9（09-02）手机端侧边栏

**【归纳】**

1. 手机端侧边栏默认隐藏，向右滑显示，5 秒后自动隐藏；显示时**不能改变页面排版布局**；电脑端不变。
2. 修复「我的分类」二级分类图标被拉成椭圆（非圆）。

### v10（09-02）手势冲突

**【归纳】** 手机右滑呼出抽屉被系统的「边缘滑动返回」抢走，要求解决冲突。

### v11（09-02）版本更新感知

**【归纳】** 线上已更新但手机刷新后仍是旧版，要求根治（后加 `updateViaCache:'none'` + 顶部「发现新版本·点击刷新」条）。

### v12（09-03）底部标签栏

**【归纳】** 手机端改底部固定标签栏：根 4 项（每日计划 / 小红书 / 英语 / **物品收纳**），点后三项进入分区子栏；电脑端仍用左侧边栏。

### v13（09-03）物品统计 4 项

**【归纳】**

1. 从交付包参考截图重新抠图（户外用品起 12 个分类 107 个图标）。
2. 系统分类 chip 增加鼠标拖拽滚动 + 滚轮横向滚动。
3. 子分类图标选择器：图标下方增加文字标签；chip 同样支持拖拽滚动。
4. 添加物品的图标选择器按截图重绘：标题+副标题+关闭 X、搜索框、横向 chip（全部/我的分类/系统一级分类）、4 列图标网格+标签。

### v14（09-03）物品统计 3 项

**【归纳】**

1. 修手机端底部弹层被底边栏遮挡（弹不出来 / 最下面一行点不到）。
2. 修正视图切换按钮文字（当前清单视图显示「清单」，网格视图显示「画廊」，之前写反）。
3. **物品详情页从弹层改为全屏档案页** `#i-subpage-detail`，按截图重绘：顶部返回+「物品档案」标题；大卡片（批量标签、图标、名称、收藏星、日均成本）；三列统计（当前库存/总入库量/已取用）；已入库天数+进度条；「库存档案」列表；底部固定操作栏（入库/取用/编辑/分享/删除）。

### v15（09-03）重新抠图

**【归纳】** 你提供了 11 张手机端系统分类截图（户外运动/服务项目/虚拟资产/固定资产/车辆用品/家装五金/宠物用品/收藏爱好/健康医疗/交通出行/母婴儿童），要求重新抠图。

### v16（09-03）性能与数据安全

**【归纳】**

1. 手机端加载很慢（SW 一次性预缓存 254 个文件）→ 改为只缓存 21 个核心文件 + 图标按需缓存。
2. 物品数据会丢失 → save 时同时写 `_backup` 备份键，load 时主键为空自动从备份恢复。

### v17（09-04）添加物品表单改版

**【归纳】**

1. **分类选择改滚轮**（截图1）：单个/批量点「分类」弹底部滚轮，仅一级分类；中间选中项黑色大字、上下分隔线；取消灰 / 确定绿(#05C160)。
2. **图标选择器改版**（截图2）：chips 第一行 = 全部/自定义/系统一级（选中深绿 #4A664F）；选「自定义」出第二行 chips；网格 4 列图标+文字标签；右上角关闭按钮改圆形灰底。

### v18（09-04）表单 3 项修复

**【归纳】**

1. 滚轮横线要跟随选中项移动（原来固定在容器中心）。
2. 分类行进入时要回显选中分类名（原来一直显示「请选择分类」）。
3. 确定按钮被底边栏遮挡，点不到。

### v19（09-04）★ 物品档案页重构 + 多批次数据模型

**【归纳】**（本版是数据模型的分水岭）

- 数据模型从「单次入库」重构为「多批次跟踪」：`item.batches[]`（入库批次）+ `item.usings[]`（取用记录）。
- **UI 1 库存档案页**（截图1）：当前库存/总入库量/已取用、入库天数、使用进度条。
- **UI 2 补货入库弹窗**（截图2）：时间默认今天、数量必填、单价选填、有效期必填 + 备注（<300字）+ 取消/确认入库（绿色高亮）。
- **UI 3 取用库存弹窗**（截图3/4/5）：默认选最新批次；批次点开弹卡片显示所有批次+可用数量；日期点开弹 3 列滚轮（2020-2060，支持闰年）；备注 <300 字。
- **UI 4 编辑库存品子页**（截图6/7）：卡片1 名称(22px)+选择图标+分类行；卡片2 入库信息（数量只读/单价总价可编辑联动/入库批次只读可点开）；卡片3 存放位置。**删除**：首批有效期、备注说明、图片上传。
- **UI 5** 分享弹「该功能暂未启用」；删除弹原生 confirm（强调手机端和电脑端都会同步删除）。
- **UI 6** 跨设备同步：工具栏新增「同步」按钮，导出/导入 JSON（导入按 id 合并）。

### v20（09-04）

**【归纳】** 「有效期弹框被补货入库页面盖住，没法维护数据」→ z-index 冲突。

### v21（09-04）多项修复

**【归纳】**

1. 每录入一条就生成一条 / 同日入库合并不生效（根因：`mergeSameDayBatches` 用 `map[key]` 错误访问 Map，应 `map.get(key)`）。
2. 补货保存后 toast 被遮挡不显示。
3. 编辑页「入库批次列表」已取用数量算错。
4. 历史数据里每条入库独立成行 → 加载时统一合并。
5. 编辑页底边栏遮挡底部按钮。
6. 编辑页入库信息行横向溢出。

### v22（09-04）云端双向同步

**【归纳】** 手机端和电脑端要真正互通（此前只有手动导出导入）。选定方案：用 GitHub 仓库做云端中转，实现「从云端下载 / 同步到云端」，按 `updatedAt` 取新合并，**删除也要跨端同步**（tombstone 机制）。

### v23（09-04）三界面 UI 优化

**【归纳】**

1. **物品统计页**：标题整行居中自适应；总资产卡片瘦身；「我的物品」行强制单行；清单卡片结构改造（横线与下方数据左边缘对齐卡片左边）；**新增置顶功能**（五角星改为占位，最多置顶 10 个，置顶卡左侧 3px 主色标记条）；收藏星统一提示「收藏功能暂未启用」。
2. **添加物品页**：标题换行居中；时间线 4 行对齐（4 个图标同 x、首字同 x、「退役日期（选填）」不换行）；日期弹窗确认按钮加高。
3. **我的分类页**：搜索框右侧加圆形放大镜；**模糊搜索**（如「笔记」命中「笔记本电脑」，「脑本」不命中）；无结果时出「未找到」卡片（我的分类可一键新增，系统分类只读）。

### v24（09-04）

**【归纳】** ①「我的分类」标题换行居中；②系统分类也复用同款「未找到」卡片（你确认该卡片在「我的分类」下表现好）。

### v25（09-05）档案页优化 + 致命 bug

**【归纳】**

1. ⚠️ 修「同步到云端」误删本机数据（doPush 错误地把云端删除标记写回本机）。
2. 物品档案页标题换行居中。
3. **购买均价改为「填写值、不重算」**（新建=填写单价，编辑=最新批次单价）。
4. 档案页顶部大卡片缩短，三列统计融入大卡片。

### v26（09-05）同步诊断

**【归纳】** 「手机有数据，点同步到云端后云端还是空的，电脑也下载不下来」→ 要求：①同步错误可视化（不再只闪 2 秒 toast）+ 测试连接按钮 ②后端可切换 GitHub / **Gitee** ③弱网自动重试。

### v27（09-05）

**【归纳】** 修「编辑物品会覆盖历史入库批次」：编辑应**追加**一条新批次，而不是用表单批次整体覆盖；取用记录不能被清空。

### v28（09-06）七项改造

**【归纳】**

1. 我的分类-系统分类「未找到」卡片要占满整行、右对齐。
2. 单个物品档案：**取消「入库」标识及其对应逻辑**。
3. 唯一标识按入库日期生成（确认）+ 单个表单「购入日期」改「入库日期」统一术语。
4. 库存档案的「添加时间/首次入库时间」可点击选择批次。
5. 单个添加：「退役日期」改「退库日期」+ 退库逻辑（已退库不计入总资产/日均成本）。
6. 批量添加：增加「退库日期（选填）」行。
7. 编辑页标题换行居中。

### v29（09-06）★ 你的纠正：改错了

**【归纳】** 你指出第 2 条改错，要求**先还原**。我误把「入库标识」当成详情页 hero 左上角&#x7684;**「批量」角标**并删除——这是**错的**。

### v30（09-06）★ 澄清后正确实现

**【归纳】** 你确认：「入库标识」= **物品档案页底部操作栏的「入库」按钮**（`data-action="stock"`），不是「批量」角标。据此删除该按钮及其整套逻辑。

### v31（09-06）★ 再次纠正范围

**【归纳】** v30 把**单个和批量**两边的「入库」按钮都删了，但你的要求只是：**单个物品档案页不显示，批量物品档案页保留**。

> 最终规则：**「入库」按钮只在批量物品档案页显示**，显隐条件与「批量」角标完全一致。

### v32（09-06）退库日期改「按批次」模型 + 6 项明细

**【归纳】**

1. 批量档案「首次入库时间」改纯展示（取最早入库日期，不可点击选批次）。
2. 批次清单只显示唯一标识符（如 `20260904入库`）。
3. 单件编辑页新增「退库日期（选填）」行。
4. 批量编辑页按批次设退库日期（选批次 + 选日期，逐批次写）。
5. 单件档案-库存卡新增退库日期行。
6. 批量档案-库存卡退库日期按批次（选批次 → 带出该批次退库日期）。

- **统计模型**：退库日期从「整物品」改为「按批次」（`batch.retiredDate`），已退库批次不计入总资产 / 平均每日成本。

### 文档输出（09-06，不改代码）

**【原文】**

> 「读取物品收纳模块中的物品统计页面，按一下内容输出要求的文档…」— 要求按固定六部分目录，以「资深 PM + UI + 数据架构」角色输出**可直接复制到 Word 的 PRD**：Markdown 表格语法、**Hex 色号**、**具体尺寸数值**、明确计算公式、未填项用 `[待补充:XXX]` 占位。

同期另要求输出《物品收纳模块详细文档》：数据库表/字段、每页每字段每按钮逻辑、取数来源、字段存哪张表、颜色色号、标题位置、卡片高度、排版位置；暂停逻辑标注「逻辑暂停使用」并附原逻辑。

---

## 四、v33 起（可给出原文）

### v33（09-06）

**【原文】**

> 进行以下内容的修正：1、批量物品的物品档案页中，点击编辑按钮，在编辑库存品页面中，退库日期（选填）卡片中，新增一行（新增的为第一行…）"选择入库批次"…弹出的页面中清单下面是取消和确定按钮，确定默认为高亮显示…显示内容就为唯一标识符的内容。

### v34（09-06）

**【原文】**

> 进行以下内容的修正：1、批量物品…选择退库批次这一样数据…都固定显示 2、补货入库页面中…新增行"生产日期"…3、有效期逻辑更新…根据输入的生产日期加上输入的有效期进行计算 4、总价行的数据=入库数量*单价…总价也可以直接编辑…加入到购买总价里。

### v35（09-06）

**【原文】**

> 进行以下修改，1、补货入库页面中，有效期…点击确定后没有在有效期行显示计算后的数据。2、库存档案卡片，第二行退库日期…没有根据上一行入库日期选择的批次，带出对应数据维护的退库日期。3、编辑库存品页面中，退库日期（选填）卡片中，①第二行图标文字对齐…②第二行字段改为"退库日期"③第二行字段改为"入库批次"…灰色展示框…一行内显示。

### v36（09-06）★ 你质疑 BUG 一直没改

**【原文】**

> 批量物品的物品档案页中，库存档案卡片，第二行退库日期…没有根据上一行入库日期选择的批次，带出对应数据维护的退库日期。**这个BUG你一直没有改，是为什么，为什么不改，还是因为什么原因取不到数据？**

> 根因（非取数问题）：`showDetail` 把该行硬编码成「选择日期」占位符，只有手动点选批次后才更新。修复：打开批量物品档案时**自动默认选中最新批次**并带出入库日期 + 退库日期。

### v37（09-06）库存档案拆分

**【原文】**

> 批量物品，库存档案页面进行修改，1、库存档案包含字段：购买均价/购买总价/库存总量/正在使用/当前状态/首次入库时间/所属分类/存放位置/最后更新…剩下的字段放到新的卡片中…2、新增卡片"每批次档案"…字段：选择批次、添加时间、入库日期、生产日期、有效期、退库日期…③打开批量物品档案时，选择批次字段默认为空…只有选择了入库批次之后才带出数据

### v38（09-06）

**【原文】**

> 批量物品档案页面，每批次档案卡片中，退库日期还是没有带出来呀，我是先选了批次…点了编辑，编辑了相同批次的退库日期数据…再次进入…发现退库日期还是没有带出来。修改这个BUG

> 根因：`openEditItem` 判定单件用「批次≤1 且 总入库≤1」，`saveEdit` 却只用「批次≤1」→ 判定不一致，误走单件分支把值清空。

### v39（09-07）

**【原文】**

> 进行以下修改 1、补货入库页面中，在有效期和入库数量中间增加退库日期字段…2、编辑库存品页面中，入库信息卡片中，只保留物品数量、入库批次两个字段。3、编辑库存品页面中，在入库信息卡片下新增每批次档案卡片…选择批次 / 物品单价 / 物品总价 / 退库日期…④ 删除退库日期（选填）卡片 4、（重复 v38 BUG 描述）修改这个BUG

### v40（09-07）修 v39 回归

**【原文】**

> 1、补货入库弹窗的「退库日期」字段，点击后没有反应。  
> 2、批量物品档案中，点击底边栏的编辑按钮后没有反应

> 根因：① `bindRestockEvents` 漏绑该行 click，且 `saveRestock` 把 `retiredDate` 硬编码成 `''`；② v39 的补丁只改了 `items.js`，**漏把「每批次档案」卡片 HTML 加进 index.html**，`openEditItem` 引用不存在的元素抛错，编辑页根本没打开。

### v41（09-08）最新一次

**【原文】**

> 进行以下修改：  
> 1、取用库存页面中，取用批次这个，选择的数据要在一行中显示，不要换行显示  
> 2、单个物品档案页面中，删除当前库存卡片，总入库量卡片  
> 3、单个物品档案页面中，已入库天数卡片中，删除已使用字段，删除进度条，将已入库天数放到当已取用卡片左边，两个卡片均分这个位置，已入库天数卡片高度=已取用卡片高度  
> 4、添加物品单个物品界面，时间线卡片，增加质保期（选填）字段，同样调用日期选择模块  
> 5、添加物品单个物品界面，计算方式卡片删除，但是保留物品计算日均成本的逻辑。  
> 6、单个物品的物品档案页面，库存档案卡片中，若正在使用行的数据=0，则当前状态默认显示"待使用"，若正在使用行的数据=1，则当前状态默认显示"现役中"。

---

## 五、反复出现 / 被纠正的指令（易错点）

1. **「入库标识」到底指什么**（v28→v29→v30→v31 连改三轮）
   - 我第一次理解成 hero 的「批量」角标 → **错**，被要求还原；
   - 第二次理解成底部「入库」按钮，但**单个和批量一起删了** → **又错**；
   - 最终：只删单个物品档案页的「入库」按钮，批量物品保留。
   - **教训**：你说的「XX 标识 / XX 按钮」含义不确定时，应先确认是哪一个、范围是什么，别靠猜。
2. **「退库日期没带出来」报了 4 次**（v35 → v36 → v38 → v39）
   - 每次根因都不同：占位符未自动带出 → 判定条件不一致 → 保存路径漏写。
   - **教训**：反复报告的「没改」类问题，优先查**默认态/占位符未自动带数据**，而不是「功能缺失」。
3. **改 JS 忘了改 HTML**（v39 事故）
   - v39 用脚本只改了 `items.js`，漏加 index.html 的「每批次档案」卡片 → 批量物品点编辑直接无反应。
   - **教训**：跨文件的大段改动必须成对落地。
4. **删除 UI 卡片会连带打断 JS**（v41 第 5 条）
   - 删掉「计算方式」卡片后，`openAddItem` 里对 `#iCalcTime` 等元素的 `.checked` 赋值会抛错，导致整个添加页打不开——必须同步清理 JS 引用。

---

## 六、长期沉淀下来的规则（你在过程中明确的）

- **入库批次唯一标识符** = `YYYYMMDD入库`（按入库日期生成，**不是 uuid**）；同一物品同一天多次入库合并为一条批次、数量相加。
- **编辑 = 追加**：编辑物品时追加一条新批次，绝不覆盖历史批次、不清空取用记录。
- **退库日期按批次**：存在 `batch.retiredDate` 上；已退库批次不计入总资产 / 平均每日成本。
- **「入库」按钮只在批量物品档案页显示**，显隐条件与「批量」角标一致（单批次且总入库 ≤1 时隐藏）。
- **「批量」角标必须保留**（曾误删，已还原）。
- 每次改动都要给**手机 + 电脑访问链接**（实为同一个 GitHub Pages 链接，加 `?v=N` 强刷缓存）。
- 改了 JS/CSS **必须 bump `sw.js` 的 CACHE 版本**，否则已装 PWA 因 cache-first 永远拿旧文件。

---

## 七、仍未解决 / 待你决定的事项

1. **云端同步（GitHub API 不可达）**：你的设备访问不到 `api.github.com`（站点能开 ≠ API 能通），v26 已内置 Gitee 后端，但需要你建码云仓库 + 私人令牌（projects 权限）并填进 `js/sync-config.js`。
2. **v41 第 2/3 条的作用范围**：档案页 hero 是单个/批量**共用**的，删除「当前库存」「总入库量」后**批量物品也看不到这两项**了。如果批量物品仍需显示，我可以按单个/批量分别控制。
3. **「当前状态」>1 件的取值**：你只说了 0→待使用、1→现役中，我目前实现为 `≤0 待使用 / ≥1 现役中`。若 >1 件要显示别的（如「多件在用」），告诉我改。
4. **Token 安全**：`js/sync-config.js` 里的 GitHub Token 会随公开站点暴露，建议换「仅限本仓库、Contents 读写」的细粒度 Token 并定期轮换。

---

*整理时间：2026-09-08 ｜ 覆盖版本：模块创建 → v41*
